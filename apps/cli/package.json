{
  "name": "@cthutool/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun build src/index.ts --outdir dist --target node --watch",
    "build": "../../scripts/build-cli-dist.sh",
    "test": "../../scripts/run-bun.sh test --timeout 30000 --preload ./tests/setup.ts",
    "test:cov": "../../scripts/run-bun.sh test --timeout 30000 --coverage --coverage-reporter=lcov --coverage-reporter=text --coverage-dir=coverage --preload ./tests/setup.ts",
    "typecheck": "../../scripts/run-bun.sh x tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "@clack/prompts": "^0.8.2",
    "archiver": "^7.0.1",
    "citty": "^0.1.6",
    "cli-progress": "^3.12.0",
    "fflate": "^0.8.2",
    "neverthrow": "^8.2.0",
    "p-limit": "^7.3.0",
    "picocolors": "^1.1.1",
    "playwright": "^1.60.0",
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@types/archiver": "^7.0.0",
    "@types/cli-progress": "^3.11.6",
    "bun-types": "^1.3.11"
  }
}
