interface:
  display_name: "Persistent Task"
  short_description: "Durable phase plans for long-running tasks"
  default_prompt: "Use $cthu-codex:persistent-task to manage this long-running task one confirmed phase at a time."

policy:
  allow_implicit_invocation: false
