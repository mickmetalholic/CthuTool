---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# CthuTool Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-22

## Active Technologies
- N/A（无应用数据持久化；配置为仓库内文件） (002-commit-convention-workflow)
- Node.js >= 20（仓库 `engines`），TypeScript 5.9.x + `@biomejs/biome`（新增，作为格式化+lint 统一引擎）、`husky`（已存在）、`turbo`（已存在） (003-biome-quality-gates)
- N/A（仅仓库配置与工作流） (003-biome-quality-gates)
- TypeScript 5.9.x + Node.js >= 20 + NestJS CLI (`@nestjs/schematics` 生成)、NestJS 核心运行时、`@biomejs/biome`（仓库已配置） (004-web-server-subapp)
- N/A（本特性仅服务启动与健康探测） (004-web-server-subapp)
- TypeScript 5.9.x（运行时 Bun） + Bun、citty、@clack/prompts、boxen、picocolors、react、ink、ink-spinner、valibot、neverthrow (005-cli-greeting-demo)
- N/A（单次 CLI 会话，无持久化） (005-cli-greeting-demo)
- Node.js（仓库 `engines`：`>=24.0.0 <25`，Volta 锁定 24.14.1）；包管理 pnpm 9.x + `husky`（已有）、`@biomejs/biome`（已有）、**`lint-staged`（新增）**、`@commitlint/cli`（已有） (006-commit-lint-auto-fix)
- N/A（仅 Git 暂存区与工作区文件） (006-commit-lint-auto-fix)
- TypeScript 5.9.x（运行时 Bun） + Bun、citty、@clack/prompts（交互式多选）、valibot、neverthrow、picocolors（可选，用于一致的错误提示） (007-cli-run-scripts)
- N/A（脚本与元数据为仓库内文件；无应用级持久化） (007-cli-run-scripts)
- TypeScript 5.9.x（Bun 运行时，Node 兼容 API） + `citty`, `@clack/prompts`, `neverthrow`, `valibot`, `cli-progress`, `picocolors/chalk`, `archiver`, `p-limit`, `fflate`（或同类 zip 读写库），`pdfjs-dist`, `@napi-rs/canvas`（或兼容 canvas 实现），`puppeteer`（仅 ePub 兜底） (008-add-cbz-converter)
- N/A（仅本地文件系统，临时目录 + 输出目录） (008-add-cbz-converter)
- TypeScript 5.9.x（Bun 运行时，Node 兼容 API） + `citty`, `@clack/prompts`, `neverthrow`, `valibot`, `cli-progress`, `picocolors/chalk`, `archiver`, `p-limit`, `puppeteer`, Node `child_process` (008-add-cbz-converter)

- Node.js ≥ 20（仓库 `engines`） + `husky`, `@commitlint/cli`, `@commitlint/config-conventional`, `commitizen`, `cz-git`（均为根目录 `devDependencies`） (002-commit-convention-workflow)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Node.js ≥ 20（仓库 `engines`）

## Code Style

Node.js ≥ 20（仓库 `engines`）: Follow standard conventions

## Recent Changes
- 008-add-cbz-converter: Added TypeScript 5.9.x（Bun 运行时，Node 兼容 API） + `citty`, `@clack/prompts`, `neverthrow`, `valibot`, `cli-progress`, `picocolors/chalk`, `archiver`, `p-limit`, `puppeteer`, Node `child_process`
- 008-add-cbz-converter: Added TypeScript 5.9.x（Bun 运行时，Node 兼容 API） + `citty`, `@clack/prompts`, `neverthrow`, `valibot`, `cli-progress`, `picocolors/chalk`, `archiver`, `p-limit`, `fflate`（或同类 zip 读写库），`pdfjs-dist`, `@napi-rs/canvas`（或兼容 canvas 实现），`puppeteer`（仅 ePub 兜底）
- 007-cli-run-scripts: Added TypeScript 5.9.x（运行时 Bun） + Bun、citty、@clack/prompts（交互式多选）、valibot、neverthrow、picocolors（可选，用于一致的错误提示）

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
