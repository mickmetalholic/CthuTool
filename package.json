{
  "name": "cthutool",
  "private": true,
  "scripts": {
    "build": "pnpm exec turbo run build",
    "test": "pnpm exec jest --config jest.config.cjs && pnpm exec turbo run test",
    "test:cov": "pnpm exec jest --config jest.config.cjs --coverage --coverageReporters=text-summary --coverageReporters=lcov --coverageReporters=html --coverageReporters=json-summary && pnpm --filter @cthutool/web run test:cov",
    "check": "pnpm exec biome check .",
    "check:write": "pnpm exec biome check --write .",
    "prepare": "husky",
    "commit": "git-cz"
  },
  "config": {
    "commitizen": {
      "path": "cz-git"
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.10",
    "commitizen": "^4.3.1",
    "cz-git": "^1.12.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "turbo": "^2.8.20",
    "typescript": "5.9.2",
    "yaml": "^2.7.0"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
