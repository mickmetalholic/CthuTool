## 1. Establish the canonical ownership and setup contract

- [x] 1.1 Record the selected OpenSpec CLI version, core workflow set, four target tools, and the distinction between OpenSpec, `npx skills`, `chc codex skills`, and `codex/plugins/cthu-codex` in the AI tooling reference.
- [x] 1.2 Decide and document whether generated adapter directories remain ignored or are intentionally committed; align `AGENTS.md` and `openspec/config.yaml` with that decision without hand-editing generated workflow files.
- [x] 1.3 Define the canonical project-authored skill source tree and the idempotent setup command that links or copies it to Codex, Cursor, OpenCode, and the shared `.agents` surface.

## 2. Migrate OpenSpec adapters

- [x] 2.1 Upgrade the OpenSpec CLI used by the repository setup to the selected current version and configure the core profile for `codex`, `cursor`, `opencode`, and `agents`.
- [x] 2.2 Run the official OpenSpec initialization/update flow and regenerate the selected tool surfaces, including the shared `.agents/skills` tree and native Cursor/OpenCode outputs.
- [x] 2.3 Remove only obsolete OpenSpec-managed workflow copies and legacy generated command files; preserve unrelated project skills, Cursor Git workflows, and all `codex/plugins/cthu-codex` files.
- [x] 2.4 Verify that the generated workflow set contains `explore`, `propose`, `apply`, `update`, `sync`, and `archive`, and that generated references use the correct native invocation form for each tool.

## 3. Clean third-party and Reasonix state

- [x] 3.1 Remove the repository-local `.codex/skills/ui-ux-pro-max`, `.cursor/skills/ui-ux-pro-max`, and `.claude/skills/ui-ux-pro-max` copies, and document the verified `npx skills add nextlevelbuilder/ui-ux-pro-max-skill --skill ui-ux-pro-max` opt-in path.
- [x] 3.2 Remove the stale `reasonix.toml` dependency and migrate any required project settings to the supported `.reasonix/` JSON format without absolute workstation paths.
- [x] 3.3 Remove or untrack local Reasonix desktop topic/session metadata and update ignore rules so it is not mistaken for portable project configuration.
- [x] 3.4 Add `repo-orientation`, `project-verify`, and `review-diff` as canonical tool-neutral project skills with minimal portable frontmatter and explicit read-only/mutation boundaries.

## 4. Align project policy and documentation

- [x] 4.1 Update `AGENTS.md` and `openspec/config.yaml` with the canonical source/generated-output boundary, OpenSpec regeneration procedure, and protected business-plugin scope.
- [x] 4.2 Replace the stale `.codex/README.md` content and add the AI tooling reference covering directory ownership, setup commands, tool invocation forms, and `npx skills` third-party lifecycle.
- [x] 4.3 Document Reasonix 0.53.2 skill discovery through `.agents/skills` and `/skill <name>`, including the fallback policy if a future Reasonix version changes its shared-skill support.
- [x] 4.4 Document that commit and pull-request actions remain explicit workflows and are not invoked by the read-only baseline skills.

## 5. Verify the migration

- [x] 5.1 Run `openspec status --change apps-cli-ai-tooling-standardization --json`, `openspec validate --all`, and `openspec doctor --json`; resolve any artifact or root-health errors.
- [x] 5.2 Run the setup/check command twice and verify idempotence, generated-path ownership, absence of duplicate skill entries, and no changes under `codex/plugins/cthu-codex`.
- [x] 5.3 Verify skill discovery for Codex, Cursor, OpenCode, and Reasonix, including the expected OpenSpec core workflows and the three baseline project skills.
- [x] 5.4 Run targeted validation for changed files plus `git diff --check`, and record the final generated-file and repository-state review in the implementation handoff.

## Implementation Handoff

### Decisions applied

- OpenSpec CLI: `@fission-ai/openspec@1.8.0`, profile `core`, tools `agents,codex,cursor,opencode`.
- Generated `openspec-*` adapters remain **ignored** and are regenerated by `pnpm setup:ai-tooling` / `scripts/setup-ai-tooling.mjs`.
- Canonical baseline skills live in `skills/` and are symlinked (default) or copied (`--copy`) into `.agents`, `.cursor`, and `.opencode` skill trees.
- No project-level `.reasonix/settings.json` was required; legacy `reasonix.toml` and desktop topic metadata were removed. `.reasonix/` stays gitignored.

### Verification results

- `openspec doctor --json`: healthy root.
- `openspec validate apps-cli-ai-tooling-standardization --type change`: passed.
- `openspec validate --all`: 54 passed; 1 pre-existing failure on unrelated change `improve-notion-channel-batch-add` (left untouched per worktree/neighbor policy).
- `pnpm setup:ai-tooling` run twice: idempotent; `pnpm check:ai-tooling` passed.
- Core workflows present under `.agents/skills`, `.cursor/skills`, `.opencode/skills` with native refs (`$openspec-*` for Codex/shared, `/openspec-*` for Cursor/OpenCode).
- Baseline skills discoverable in all three surfaces; Reasonix can read `.agents/skills` via `/skill <name>`.
- `codex/plugins/cthu-codex`: unchanged.
- `git diff --check`: clean.

### Generated vs committed review

- Local generated (ignored): `.agents/`, `.opencode/`, `.cursor/skills/openspec-*`, baseline skill links under agent trees.
- Still committed intentionally: `.cursor/skills/commit`, `.cursor/skills/create-pull-request`, `skills/`, `docs/ai-tooling.md`, policy/config updates.
- Removed from repo content: legacy OpenSpec copies under `.codex`/`.claude`/`.cursor`, `ui-ux-pro-max` copies, `reasonix.toml`, tracked `.reasonix` desktop metadata.

### Review follow-up

- Updated the docs-site Engineering Config reference and the Codex README so they no longer advertise `reasonix.toml`, legacy skill roots, or the removed `chc codex doctor` command.
- `pnpm check:ai-tooling` now verifies the OpenSpec global profile/delivery, resolves each generated or linked skill directory, and requires a non-empty `SKILL.md` with the expected skill name.
