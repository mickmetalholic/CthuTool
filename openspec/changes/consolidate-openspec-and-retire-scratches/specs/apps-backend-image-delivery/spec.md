## REMOVED Requirements

### Requirement: Backend image is published to GHCR
**Reason**: Backend image publication and deployment pinning are consolidated into `apps-backend-image-ci`.
**Migration**: Use `apps-backend-image-ci` for backend image validation, publication, and deployment manifest pinning requirements.
