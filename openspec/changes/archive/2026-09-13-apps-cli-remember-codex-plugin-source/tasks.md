## 1. Source resolution and persistence

- [x] 1.1 Add a versioned, atomic user config for the remembered plugin source and source validation.
- [x] 1.2 Resolve first-use, saved, explicit override, stale saved path, and change-source flows in `chc codex install`.

## 2. Output and documentation

- [x] 2.1 Show source path/provenance and per-plugin install/cache results; distinguish zero enabled plugins.
- [x] 2.2 Document prompt, remembered default, one-run override, non-interactive setter, and output meaning.

## 3. Verification

- [x] 3.1 Add focused source-flow tests and retain existing installation coverage.
- [x] 3.2 Build committed CLI dist, run typecheck/lint/tests, validate OpenSpec, and review scoped diff.
