## 1. Error and output

- [x] 1.1 Detect `EBUSY` in cache synchronization and preserve its path.
- [x] 1.2 Emit an actionable human error and structured JSON code from `chc codex install`.

## 2. Verification

- [x] 2.1 Cover a simulated cache lock and retain ordinary-error behavior.
- [x] 2.2 Update docs, build the committed CLI bundle, validate the change, and run focused checks.
