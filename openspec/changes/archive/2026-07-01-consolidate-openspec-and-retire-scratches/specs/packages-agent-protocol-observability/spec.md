## REMOVED Requirements

### Requirement: Protocol correlation metadata
**Reason**: Protocol observability semantics are consolidated into `packages-agent-protocol`.
**Migration**: Use `packages-agent-protocol` for agent protocol metadata, compatibility, and redaction requirements.

### Requirement: Protocol metadata compatibility
**Reason**: Protocol observability semantics are consolidated into `packages-agent-protocol`.
**Migration**: Use `packages-agent-protocol` for agent protocol metadata, compatibility, and redaction requirements.

### Requirement: Protocol metadata redaction
**Reason**: Protocol observability semantics are consolidated into `packages-agent-protocol`.
**Migration**: Use `packages-agent-protocol` for agent protocol metadata, compatibility, and redaction requirements.
