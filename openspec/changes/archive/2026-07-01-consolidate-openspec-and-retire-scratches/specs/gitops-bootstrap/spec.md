## REMOVED Requirements

### Requirement: Bootstrap directory scaffold exists
**Reason**: GitOps bootstrap behavior is consolidated into `gitops-delivery`.
**Migration**: Use `gitops-delivery` for bootstrap, namespace, and ArgoCD Application delivery requirements.
