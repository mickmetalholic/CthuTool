## 1. Configuration

- [x] 1.1 Add repository-specific `context` to `openspec/config.yaml`.
- [x] 1.2 Add proposal, specs, design, and tasks rules to `openspec/config.yaml`.
- [x] 1.3 Keep `schema: spec-driven` unchanged.

## 2. Verification

- [x] 2.1 Run `openspec validate sync-openspec-project-context --strict --no-interactive`.
- [x] 2.2 Run `openspec validate --specs --strict --no-interactive`.
- [x] 2.3 Confirm no generated `.claude/`, `.codex/`, or `.cursor/` OpenSpec skill files were edited.
