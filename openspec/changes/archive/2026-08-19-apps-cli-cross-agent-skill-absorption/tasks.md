## 1. Hermes provenance and Codex-side discovery

- [x] 1.1 Define the versioned bridge provenance sidecar schema, source fingerprint rules, Codex target scopes, and the dedicated Hermes evolution marker contract.
- [x] 1.2 Implement read-only Hermes candidate discovery for the Codex bridge with bundled-manifest, Hub-lock, protected built-in, external-directory, organization-mirror, explicit opt-out, and missing-provenance exclusions.
- [x] 1.3 Ensure the bridge does not treat `created_by: "agent"`, `agent_created: true`, directory location, activity, or `author` alone as strict Evolution provenance unless Hermes explicitly documents the marker as such.
- [x] 1.4 Add fixture helpers covering eligible evolution-marked skills, unmarked local skills, bundled skills, Hub skills, external skills, organization mirrors, and explicit opt-outs.

## 2. Codex bridge skill

- [x] 2.1 Add the Codex-facing Hermes absorption skill to the repository-managed CthuCodex skill assets.
- [x] 2.2 Implement the Hermes-to-Codex inspect, adapt, preview, warning, confirmation, and cancellation workflow without executing source-provided scripts.
- [x] 2.3 Implement Codex-target provenance sidecar creation, source fingerprinting, repeat-absorption comparison, and collision choices for merge, replace, or rename.
- [x] 2.4 Add safety checks for secrets, inaccessible references, absolute paths, unsupported Hermes behavior, executable content, and incomplete source trees.
- [x] 2.5 Document that Hermes-side skill absorption is owned by the Hermes skill repository/local skill directory and is not installed or managed by CthuTool.

## 3. Codex repository promotion

- [x] 3.1 Extend the Codex skills domain model to discover valid bridge-marked local Codex skills separately from GitHub/local-source manifest reconciliation.
- [x] 3.2 Add bridge promotion candidates to the read-only JSON snapshot without exposing arbitrary unmanaged local skills.
- [x] 3.3 Add a separate `Promote absorbed skills` action to the interactive `chc codex skills` flow with a reviewed, default-negative plan.
- [x] 3.4 Implement collision validation and atomic promotion of the adapted skill and required Codex support/metadata files into `codex/plugins/cthu-codex/skills` while preserving provenance.
- [x] 3.5 Ensure promotion does not write `codex/skills.manifest.json`, invoke `npx skills`, mutate Hermes files, or alter unrelated plugin skills.

## 4. Tests and documentation

- [x] 4.1 Add unit tests for Hermes eligibility classification, sidecar validation, fingerprints, and Codex collision states.
- [x] 4.2 Add Codex bridge contract fixtures/tests for preview-only, confirmation, cancellation, source preservation, strict provenance, and unsafe-source rejection.
- [x] 4.3 Add CLI integration tests for JSON inventory, interactive promotion planning, confirmed promotion, repository collisions, and unchanged GitHub/local-source lifecycle behavior.
- [x] 4.4 Update CLI, plugin, and Codex bridge documentation to describe Hermes built-in exclusion, strict evolution provenance, and the boundary that Hermes-side management belongs elsewhere.
- [x] 4.5 Keep generated `.claude/`, `.codex/`, and `.cursor/` adapters unchanged unless regenerated by the repository's supported tooling.

## 5. Verification

- [x] 5.1 Run affected CLI lint, TypeScript checks, and tests.
- [x] 5.2 Run `git diff --check` and inspect the final change for unrelated OpenSpec or generated-file modifications.
- [x] 5.3 Run `openspec validate apps-cli-cross-agent-skill-absorption --type change --strict --no-interactive`.
